# xUnit Testing Setup

The solution contains a separate `Tests/Invenzo.Tests` xUnit project.

Exactly 25 tests are included: 5 each for ProductController, SaleController, PurchaseController, BillingController, and PaymentController.

No UI tests are included. No test requires a database, browser, HTTP server, or external API.
